version: 2

python:
  version: 3.7

sphinx:
  fail_on_warning: true

conda:
    environment: doc/environment.yml

submodules:
    include: all
    recursive: true

formats:
    - htmlzip

build:
  image: stable
